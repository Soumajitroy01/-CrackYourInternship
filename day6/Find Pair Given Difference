bool findPair(int arr[], int n, int x){
    //code
    unordered_map<int,int>mp;
    for(int i=0;i<n;i++)
    {
        if(mp[arr[i]-x] or mp[arr[i]+x]) return true;
        mp[arr[i]]++;
    }
    
    return false;
}
