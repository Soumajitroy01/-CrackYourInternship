class Solution {
public:
    bool canJump(vector<int>& nums) 
    {
        int n = nums.size();
        int lo=n-1,hi=n-1;
        while(lo>=0)
        {
            if(hi-lo<=nums[lo]) hi=lo;
            lo--;
        }
        if(hi==0) return true;
        return false;
    }
};
