class Solution {
public:
    bool hasCycle(ListNode *head) {
        ListNode *fast=head;
        while(fast!=NULL and fast->next!=NULL)
        {
            head=head->next;
            fast=fast->next->next;
            
            if(head==fast) return true;
        }
        
        return false;
    }
};
